readme_content = """# VIPThink Checker (小老师空值检查员)

这是一个专为 VIPThink 课件管理系统量身定制的 Chrome 浏览器扩展插件。它的主要功能是自动化检查课件列表中的“小老师作品”配置，精准找出漏填“建议主题”或“录制建议”的课件，并自动生成校验报告。

## ✨ 功能特点

* **解放双手，全自动排查**：一键自动遍历当前页面的所有课件，模拟人工点击“更多” -> “小老师作品”，并自动开闭弹窗，全程无需人工干预。
* **精准突破框架限制**：
  * **幽灵菜单定位**：完美适配 Element UI 的底层逻辑，精准锁定挂载在页面底层的动态下拉菜单（处理 `offsetParent !== null` 只寻找肉眼可见的菜单）。
  * **富文本空值检测**：针对 Quill 编辑器进行深度过滤，自动剔除零宽空格（`\\u200B`）等不可见字符，绝不误判“假内容”。
* **自动化报告导出**：排查结束后，如果发现有漏填项，会自动生成并下载一份 `.csv` 格式的稽核报告；如果全部合格，则会弹出成功提示。

## 📁 文件结构

安装说明
将本项目文件夹 (VIPThink_Checker) 下载或放置在你的电脑中。

打开 Chrome 浏览器，在地址栏输入 chrome://extensions/ 并回车进入。

开启页面右上角的 “慢速/高级模式”或“开发者模式” (Developer mode)。

点击页面左上角的 “加载已解压的扩展程序” (Load unpacked)。

选择 VIPThink_Checker 文件夹，即可完成安装。

💡 详细使用方法
登录并进入 VIPThink 课件管理系统中的课件列表页面 (https://jy.vipthink.cn/#/teacher_new/chapter_list)。

非常重要： 首次进入页面、切换分页或更新插件代码后，请务必按 F5 刷新当前网页，确保脚本成功注入。

点击 Chrome 浏览器右上角的拼图图标，找到“小老师空值检查员”并固定/打开。

点击控制面板中的 “🔍 开始全页检查” 按钮。

脚本运行期间注意事项：

控制面板的状态栏会实时同步显示如 正在检查第 X/Y 笔... 的进度提示。

请不要点击网页的任何地方或切换标签页，静待机器人自动模拟点击和排查完成。

排查结束后：

若发现漏填项：浏览器会自动触发下载名为 小老师漏填稽核报告_YYYY-MM-DD.csv 的文件，可用 Excel 直接打开。

若完全无漏填：系统会弹出 Alert 框提示：太棒了！当前页面的小老师作品全部都有填写主题与建议。

⚠️ 常见问题与排错 (QA)
问题 1：点击按钮后，网页没有任何动静或报错？

解答：请确认是否在进入页面后执行了 F5 刷新。如果页面是全新打开或从其他路由跳转过来的，扩展需要重新刷新网页来完成脚本注入。

问题 2：运行过程中不小心点到了网页，或者进度卡住不动了怎么办？

解答：在自动模拟点击期间人工干预可能会导致流程中断。若发生中断，请直接按下 F5 刷新网页，然后重新点开插件点击“开始全页检查”即可重新开始。

问题 3：我修改了 content.js 或其他文件里的代码，为什么网页上运行的还是旧逻辑？

解答：Chrome 插件的代码修改后需要手动重载。

前往 chrome://extensions/ 页面，找到该插件卡片，点击右下角的 “刷新 🔄” 按钮。

回到对应的课件管理网页，按下 F5 刷新当前页面。此时最新的代码才会完全生效。
"""
